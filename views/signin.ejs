<%- include('header') %>
<div class="wrap">
<div class="ui grid">
    <div class="sixteen wide column">
        <form class="ui form segment" method="post">
            <div class="field required">
                <label>用户名</label>
                <input id="signin-name" placeholder="用户名" type="text" name="name">
            </div>
            <div class="field required">
                <label>密码</label>
                <input id="signin-password" placeholder="密码" type="password" name="password">
            </div>
            <input onclick="signin()" id="submit" type="button" class="ui button fluid" value="登录">
        </form>
    </div>
</div>
</div>
<%- include('footer') %>